#!/usr/bin/env bash
# Double-click this file in Finder to start the app and open it in your browser.
cd "$(dirname "$0")"
echo "Web3 District — starting dev server..."
echo "Close this window to stop the server (or press Ctrl+C)."
bash scripts/launch-dev.sh
